apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:
jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: node
          artifact-id: 2d2011e5-5563-40ec-a8cf-d590ef0cd394
          target-environment: mm-qa-aspm